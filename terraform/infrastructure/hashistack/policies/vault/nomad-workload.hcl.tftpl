path "${kv_mount_path}/data/{{identity.entity.aliases.${auth_method_accessor}.metadata.nomad_namespace}}/{{identity.entity.aliases.${auth_method_accessor}.metadata.nomad_job_id}}/*" {
  capabilities = ["read"]
}

path "${kv_mount_path}/data/{{identity.entity.aliases.${auth_method_accessor}.metadata.nomad_namespace}}/{{identity.entity.aliases.${auth_method_accessor}.metadata.nomad_job_id}}" {
  capabilities = ["read"]
}

path "${kv_mount_path}/metadata/{{identity.entity.aliases.${auth_method_accessor}.metadata.nomad_namespace}}/*" {
  capabilities = ["list"]
}

path "${kv_mount_path}/metadata/*" {
  capabilities = ["list"]
}

path "${database_mount_path}/creds/{{identity.entity.aliases.${auth_method_accessor}.metadata.nomad_job_id}}" {
    capabilities = ["read"]
}

path "${database_mount_path}/static-creds/{{identity.entity.aliases.${auth_method_accessor}.metadata.nomad_job_id}}" {
    capabilities = ["read"]
}
