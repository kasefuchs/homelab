---
# The Vagrant Docker image to use for all virtual machines.
image: ghcr.io/kasefuchs/homelab-debian:bookworm-slim

# Default hardware configuration for each Container.
domain:
  # Additional arguments to pass to docker run when the container is started.
  create_arguments:
    - --privileged
    - --dns=127.0.0.1
