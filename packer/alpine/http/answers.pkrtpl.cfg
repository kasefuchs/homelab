KEYMAPOPTS="us us"
HOSTNAMEOPTS="${hostname}"
DEVDOPTS="mdev"
INTERFACESOPTS="auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp
hostname alpine
"
DNSOPTS="${dns}"
TIMEZONEOPTS="${timezone}"
PROXYOPTS="none"
APKREPOSOPTS="-1 -c"
USEROPTS="-a -u ${username}"
SSHDOPTS="openssh"
NTPOPTS="chrony"
DISKOPTS="-L -m sys -s 0 /dev/vda"
