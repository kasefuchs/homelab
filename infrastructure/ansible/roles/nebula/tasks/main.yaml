---
- name: Download Nebula
  tags: download
  ansible.builtin.include_tasks:
    file: download.yaml
    apply:
      tags: download
      become: false
      run_once: true
      delegate_to: localhost

- name: Install Nebula
  tags: install
  ansible.builtin.include_tasks:
    file: install.yaml
    apply:
      tags: install
