---
# Base local directory for storing Vault certificates.
vault_certificate_local_dir: "{{ (vault_main_file_local_dir, 'certificate', (inventory_dir | ansible.builtin.basename)) | ansible.builtin.path_join }}"

# Directory for storing Vault CA files.
vault_certificate_ca_local_dir: "{{ (vault_certificate_local_dir, 'ca') | ansible.builtin.path_join }}"
