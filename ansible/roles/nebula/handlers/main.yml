---
- name: Reload Monit
  ansible.builtin.command:
    argv:
      - "{{ monit_binary_path }}"
      - reload
      - -c{{ monit_config_path }}

- name: Monitor Nebula
  ansible.builtin.command:
    argv:
      - "{{ monit_binary_path }}"
      - monitor
      - nebula
      - -c{{ monit_config_path }}

- name: Restart Nebula
  ansible.builtin.command:
    argv:
      - "{{ monit_binary_path }}"
      - restart
      - nebula
      - -c{{ monit_config_path }}
