---
- name: Enable Nebula service (SystemD)
  when: ansible_service_mgr == "systemd"
  listen: Enable Nebula service
  ansible.builtin.systemd_service:
    name: nebula
    enabled: true

- name: Restart Nebula service (SystemD)
  when: ansible_service_mgr == "systemd"
  listen: Restart Nebula service
  ansible.builtin.systemd_service:
    name: nebula
    state: restarted
