---
- name: Create local directories
  become: false
  run_once: true
  delegate_to: localhost
  ansible.builtin.file:
    path: "{{ item }}"
    mode: "0755"
    state: directory
  loop:
    - "{{ vault_cache_download_dir }}"
    - "{{ vault_cache_dir }}"
    - "{{ vault_secrets_dir }}"
