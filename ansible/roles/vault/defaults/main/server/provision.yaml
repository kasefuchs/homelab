---
# Host where Vault will be provisioned.
vault_server_provision_host: "{{ groups['vault-servers'][0] }}"

# API address used to provision Vault.
vault_server_provision_api_address: "https://{{ ansible_default_ipv4.address }}:{{ vault_server_config_listen_api_port }}"
