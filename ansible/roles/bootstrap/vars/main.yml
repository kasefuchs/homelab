---
# Map of packages for installation, broken down by distribution.
bootstrap_package_distribution_install:
  default:
    - unzip
    - python3
    - python3-pip
    - python3-venv

  centos:
    - unzip
    - python3
    - python3-pip
