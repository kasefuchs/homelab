---
# Directory where Pebble data will be stored.
pebble_config_state_dir: /var/lib/pebble

# Directory where Pebble layers config files will be stored.
pebble_config_layer_dir: "{{ (pebble_config_state_dir, 'layers') | ansible.builtin.path_join }}"
