---
- name: Copy Traefik configuration
  ansible.builtin.template:
    src: traefik.yaml.j2
    dest: /etc/traefik.d/traefik.yaml
    owner: traefik
    group: traefik
    mode: "0644"
  notify: Restart Traefik

- name: Ensure handlers to run
  ansible.builtin.meta: flush_handlers
