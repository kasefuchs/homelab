---
- name: Remove DNS resolvers
  ansible.builtin.apt:
    pkg:
      - resolvconf
      - openresolv
      - systemd-resolved
    state: absent

- name: Remove unused packages
  ansible.builtin.apt:
    autoremove: true
