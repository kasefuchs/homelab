---
# Base local directory for storing Consul bootstrap data.
consul_bootstrap_local_dir: "{{ (consul_main_file_local_dir, 'bootstrap') | ansible.builtin.path_join }}"

# Full path to the local file where Consul bootstrap output will be saved.
consul_bootstrap_encrypted_local_file: "{{ (consul_bootstrap_local_dir, common_inventory_name ~ '.sops.json') | ansible.builtin.path_join }}"
