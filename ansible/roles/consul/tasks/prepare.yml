---
- name: Create local directories
  become: false
  run_once: true
  delegate_to: localhost
  ansible.builtin.file:
    path: "{{ item }}"
    mode: "0755"
    state: directory
  loop:
    - "{{ consul_cache_download_dir }}"
    - "{{ consul_cache_dir }}"
    - "{{ consul_secrets_dir }}"
