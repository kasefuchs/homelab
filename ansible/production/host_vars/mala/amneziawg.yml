amneziawg_config_interface:
  Address: 10.45.32.16/24, fd45:45:45:20::16/64
  ListenPort: 48526
  PrivateKey: "{{ amneziawg_config_private_key }}"
  PostUp: sysctl -w net.ipv4.ip_forward=1; sysctl -w net.ipv6.conf.all.forwarding=1
amneziawg_config_peers:
  # silky
  - PublicKey: "{{ amneziawg_config_public_keys.silky }}"
    PresharedKey: "{{ amneziawg_config_preshared_keys.silky }}"
    AllowedIPs: 10.45.0.0/16, fd45:45:45::/48
    Endpoint: silky.kasefuchs.net:48092
  # apex
  - PublicKey: "{{ amneziawg_config_public_keys.apex }}"
    PresharedKey: "{{ amneziawg_config_preshared_keys.apex }}"
    AllowedIPs: 10.45.224.16/32, fd45:45:45:e0::16/128
