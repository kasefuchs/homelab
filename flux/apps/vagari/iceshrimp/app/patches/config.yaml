apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: iceshrimp
spec:
  values:
    config:
      Instance:
        WebDomain: shrimp.vagari.homelab.test
        AccountDomain: shrimp.vagari.homelab.test
      Storage:
        ObjectStorage:
          AccessUrl: https://iceshrimp.web.garage.vagari.homelab.test/
          DisableValidation: true
      # Allow private Storage::ObjectStorage AccessUrl endpoint
      Security:
        AllowLocalIPv4: true
        AllowLocalIPv6: true
