apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: iceshrimp
spec:
  values:
    config:
      Instance:
        WebDomain: shrimp.vagari.homelab.test
        AccountDomain: shrimp.vagari.homelab.test
      Storage:
        ObjectStorage:
          AccessUrl: http://iceshrimp.web.garage.vagari.homelab.test/
      # Allow private Storage::ObjectStorage AccessUrl endpoint
      Security:
        AllowLocalIPv4: true
        AllowLocalIPv6: true
