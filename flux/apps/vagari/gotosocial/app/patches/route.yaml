apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gotosocial
spec:
  values:
    route:
      main:
        enabled: true
        hostnames:
          - social.vagari.homelab.test
        parentRefs:
          - name: external
            namespace: gateway-system
            sectionName: https
