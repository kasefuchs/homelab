apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: home-assistant
spec:
  values:
    route:
      main:
        enabled: true
        hostnames:
          - home.kind.homelab.test
        parentRefs:
          - name: external
            namespace: gateway-system
            sectionName: https
