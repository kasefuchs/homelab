apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: s-ui
spec:
  values:
    ingress:
      main:
        annotations:
          external-dns.alpha.kubernetes.io/target: ingress.kind.homelab.test
        hosts:
          - host: sui.kind.homelab.test
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: main
                  port: web
              - path: /sub/
                pathType: Prefix
                service:
                  identifier: main
                  port: sub
