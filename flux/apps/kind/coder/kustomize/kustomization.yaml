apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/coder/kustomize
patches:
  - path: patches/ingress.yaml
  - path: patches/environment.yaml
    target:
      group: helm.toolkit.fluxcd.io
      kind: HelmRelease
      name: coder
