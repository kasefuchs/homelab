apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gotosocial
spec:
  values:
    config:
      host: social.kind.homelab.test
      protocol: http
      advanced-rate-limit-requests: 0
      storage-s3-redirect-url: http://gotosocial.web.garage.kind.homelab.test
      storage-s3-proxy: false
