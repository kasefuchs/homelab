apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: iceshrimp
spec:
  values:
    controllers:
      main:
        initContainers:
          config:
            env:
              ICESHRIMP_S3_ACCESS_URL: http://iceshrimp.web.garage.homelab.internal/
              ICESHRIMP_WEB_DOMAIN: shrimp.homelab.internal
              ICESHRIMP_ACCOUNT_DOMAIN: shrimp.homelab.internal
