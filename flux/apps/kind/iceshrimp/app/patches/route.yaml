apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: iceshrimp
spec:
  values:
    route:
      main:
        enabled: true
        hostnames:
          - shrimp.kind.homelab.test
        parentRefs:
          - name: external
            namespace: gateway-system
            sectionName: https
