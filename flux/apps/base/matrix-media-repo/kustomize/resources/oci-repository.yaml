apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: matrix-media-repo
spec:
  interval: 1h
  url: oci://codeberg.org/kasefuchs/helm-charts/matrix-media-repo
  ref:
    semver: "0.2.*"
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
