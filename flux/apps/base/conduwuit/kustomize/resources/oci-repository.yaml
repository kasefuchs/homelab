apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: conduwuit
spec:
  interval: 1h
  url: oci://codeberg.org/kasefuchs/helm-charts/conduwuit
  ref:
    semver: "0.1.*"
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
