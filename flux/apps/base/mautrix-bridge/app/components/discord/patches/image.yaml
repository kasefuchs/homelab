apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mautrix-bridge
spec:
  values:
    controllers:
      main:
        containers:
          main:
            image:
              tag: v0.7.7
