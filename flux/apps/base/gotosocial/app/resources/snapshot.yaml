apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotPolicy
metadata:
  name: gotosocial-db
spec:
  repository:
    kind: ClusterRepository
    name: shared
  credentialProjection:
    enabled: true
  copyMethod: Direct
  sources:
    # pvcSelector is not fully wired in kopiur yet
    # falling back to explicit pvc name
    - pvc:
        name: gotosocial-db-1
  retention:
    keepLatest: 3
    keepDaily: 7
    keepWeekly: 2
  mover:
    inheritSecurityContextFrom:
      pvcConsumer: {}
  compression:
    compressor: zstd
  hooks:
    beforeSnapshot:
      - workloadExec:
          podSelector:
            matchLabels:
              cnpg.io/cluster: gotosocial-db
              cnpg.io/instanceRole: primary
          container: postgres
          command:
            - "psql"
            - "-U"
            - "postgres"
            - "-c"
            - "SELECT pg_backup_start('kopiur');"
          timeout: 2m
    afterSnapshot:
      - workloadExec:
          podSelector:
            matchLabels:
              cnpg.io/cluster: gotosocial-db
              cnpg.io/instanceRole: primary
          container: postgres
          command:
            - "psql"
            - "-U"
            - "postgres"
            - "-c"
            - "SELECT pg_backup_stop();"
          timeout: 2m
          continueOnFailure: true
---
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotSchedule
metadata:
  name: gotosocial-db-nightly
spec:
  policyRef:
    name: gotosocial-db
  schedule:
    cron: "H 2 * * *"
    jitter: 30m
    runOnCreate: false
