apiVersion: garage.rajsingh.info/v1alpha1
kind: GarageBucket
metadata:
  name: nextcloud-garage-bucket
spec:
  globalAlias: nextcloud
  clusterRef:
    name: garage
    namespace: garage
  quotas:
    maxSize: 20Gi
  website:
    enabled: true
