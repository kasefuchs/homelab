apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: xray
spec:
  interval: 1h
  url: oci://codeberg.org/kasefuchs/helm-charts/xray
  ref:
    tag: "0.2.2"
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
