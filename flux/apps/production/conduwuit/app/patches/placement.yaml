apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: conduwuit
spec:
  values:
    controllers:
      main:
        pod:
          nodeSelector:
            topology.kubernetes.io/region: eu-central
