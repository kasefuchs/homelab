apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: iceshrimp
spec:
  values:
    controllers:
      main:
        pod:
          nodeSelector:
            topology.kubernetes.io/region: eu-central
