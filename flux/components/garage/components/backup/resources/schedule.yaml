apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotSchedule
metadata:
  name: ${NAME}-garage
spec:
  policyRef:
    name: ${NAME}-garage
  schedule:
    cron: "H 2 * * *"
    jitter: 30m
    runOnCreate: false
