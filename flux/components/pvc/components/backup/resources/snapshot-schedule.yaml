apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotSchedule
metadata:
  name: ${NAME}
spec:
  policyRef:
    name: ${NAME}
  schedule:
    cron: H 2 * * *
    jitter: 30m
    runOnCreate: false
