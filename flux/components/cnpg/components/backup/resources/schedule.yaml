apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotSchedule
metadata:
  name: ${NAME}-db
spec:
  policyRef:
    name: ${NAME}-db
  schedule:
    cron: "H 2 * * *"
    jitter: 30m
    runOnCreate: false
