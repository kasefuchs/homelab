apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
spec:
  values:
    ingressRoute:
      dashboard:
        enabled: true
        matchRule: Host(`traefik.kind.local`)
        entryPoints: [websecure]
