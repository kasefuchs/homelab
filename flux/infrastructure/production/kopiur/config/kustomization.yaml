apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/kopiur/config
  - resources/secret.yaml
patches:
  - path: patches/backend.yaml
  - path: patches/schedule.yaml
    target:
      group: kopiur.home-operations.com
      kind: ClusterRepository
