apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-operator
spec:
  values:
    web:
      ingress:
        enabled: true
        annotations:
          external-dns.alpha.kubernetes.io/target: ingress.vagari.homelab.test
        hosts:
          - host: flux.vagari.homelab.test
            paths:
              - path: /
                pathType: Prefix
