apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-operator
spec:
  values:
    web:
      httpRoute:
        enabled: true
        hostnames:
          - flux.vagari.homelab.test
        parentRefs:
          - name: internal
            namespace: gateway-system
            sectionName: https
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
