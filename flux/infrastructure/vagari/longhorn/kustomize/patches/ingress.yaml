apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
spec:
  values:
    ingress:
      enabled: true
      annotations:
        external-dns.alpha.kubernetes.io/target: ingress.vagari.homelab.test
      host: longhorn.vagari.homelab.test
