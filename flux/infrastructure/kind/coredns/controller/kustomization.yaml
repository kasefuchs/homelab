apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/coredns/controller
patches:
  - path: patches/service.yaml
  - path: patches/server.yaml
    target:
      group: helm.toolkit.fluxcd.io
      kind: HelmRelease
