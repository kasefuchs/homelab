apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/gateway-api/config
  - resources/certificate.yaml
patches:
  - path: patches/tls.yaml
    target:
      group: gateway.networking.k8s.io
      kind: Gateway
