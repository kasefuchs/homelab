apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns
spec:
  values:
    provider:
      name: rfc2136
    extraArgs:
      - --rfc2136-host=dns
      - --rfc2136-port=53
      - --rfc2136-zone=homelab.local
      - --rfc2136-insecure
      - --rfc2136-tsig-axfr
