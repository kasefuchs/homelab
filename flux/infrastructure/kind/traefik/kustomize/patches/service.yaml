apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
spec:
  values:
    service:
      annotations:
        external-dns.alpha.kubernetes.io/hostname: traefik.homelab.local
