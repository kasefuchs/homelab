apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: trust-manager-controller
  namespace: flux-system
spec:
  path: infrastructure/base/trust-manager/controller
  wait: true
  prune: true
  timeout: 10m
  interval: 1h
  retryInterval: 2m
  targetNamespace: cert-manager
  sourceRef:
    kind: OCIRepository
    name: flux-system
  dependsOn:
    - name: cert-manager-controller
  decryption:
    provider: sops
    secretRef:
      name: sops-age
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: trust-manager-config
  namespace: flux-system
spec:
  path: infrastructure/base/trust-manager/config
  wait: true
  prune: true
  timeout: 10m
  interval: 1h
  retryInterval: 2m
  targetNamespace: cert-manager
  sourceRef:
    kind: OCIRepository
    name: flux-system
  dependsOn:
    - name: trust-manager-controller
    - name: cert-manager-config
  decryption:
    provider: sops
    secretRef:
      name: sops-age
