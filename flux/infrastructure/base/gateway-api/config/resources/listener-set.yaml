apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: external
spec:
  parentRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: external
  listeners: []
---
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: internal
spec:
  parentRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: internal
  listeners: []
