apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-to-https
spec:
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
  parentRefs:
    - name: external
      sectionName: http
